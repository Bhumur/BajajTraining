spring:
  application:
    name: Microservice_2

server:
  port: 8051

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
