package com.exercise.automobiles;

import com.exercise.iface.Automobile;

public class ShowRoom {
	
	public Automobile autoFactory(int choice, String color, double price, String model) {
		Automobile automobile = null;
		switch(choice) {
			case 1:{
				automobile = new MarutiCar(color, price, model);
				break;
			}
			case 2:{
				automobile = new PulsarBike(color, price, model);
				break;
			}
		}
		if(choice==1)
			return getMarutiCar(color, price, model);
		else
	}
	
	public MarutiCar getMarutiCar(String color, double price, String model) {
		return 
	}
	
	public PulsarBike getPulsarBike(String color, double price, String model) {
		return new PulsarBike(color, price, model);
	}
	public String printQuotation() {
		return 
	}
}
