spring:
  application:
    name: reviewService
  config:
    import: "configserver:http://localhost:8888"

server:
  port: 5070

management:
  endpoints:
    web:
      exposure:
        include: "*"
